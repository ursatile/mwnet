@page
@model Rockaway.WebApp.Pages.ArtistsModel

@foreach (var artist in Model.Artists) {
	<section>
	@Html.DisplayFor(_ => artist)
	<a asp-page="Artist" asp-route-slug="@artist.Id">more info...</a>
	</section>
}
